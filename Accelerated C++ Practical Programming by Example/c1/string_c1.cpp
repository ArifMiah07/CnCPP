// Variables can be defined in one of three ways:

#include<iostream>
#include<string>

int main()
{

    // 1
    // 2
    std::string stars(100, '*');
    // 3
    std::string name;
    //1
    std::string hello = "Hello";
    const std::string message = hello + ", world" + "!";

    return 0;
}

// exp1 this is ocde is valid
